{
  "name": "janus-orchestrator-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for Janus cognitive steering — Sol/Nox mode switching, personality routing, bias analysis, and perspective merging",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
