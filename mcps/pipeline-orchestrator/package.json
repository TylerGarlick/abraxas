{
  "name": "pipeline-orchestrator-mcp",
  "version": "1.0.0",
  "description": "MCP server for autonomous task pipeline orchestration - the 'Brain' for managing workers and task discovery",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
