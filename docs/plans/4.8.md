# 📉 Abraxas State Audit: Discrepancy Report (v4.8)

**Audit Date**: 2026-05-28
**Auditor**: Mary Jane (Sovereign Mode)
**Scope**: Filesystem $\leftrightarrow$ ArangoDB $\leftrightarrow$ Runtime $\leftrightarrow$ Roadmap (v4.3-v4.6)

---

## 🚨 Critical Discrepancies (The "Void" Map)

### 1. The "Aspirational" Gap (Plans vs. Implementation)
A massive delta exists between the Roadmap (v4.3 - v4.6) and the current operational state of the `projects/abraxas/skills` directory.

| Plan Component | Roadmap Status | Implementation Status | Discrepancy |
| :--- | :--- | :--- | :--- |
| **Harmonia** | v4.3 P1 | Folder exists, no `mcp_tools.py` logic | **HOLLOW**. Orchestration logic is missing. |
| **Krisis** | v4.3 P1 | Folder exists, no `mcp_tools.py` logic | **HOLLOW**. Ethos-weighting is missing. |
| **Aporia** | v4.3 P1 | **Sovereign Void** | **MISSING**. Directory does not exist. |
| **Sieve** | v4.3 P2 | Not in `skills/` | **MISSING**. Signal pipeline is offline. |
| **Auto-Agon** | v4.3 P3 | Folder exists, no logic | **HOLLOW**. Stress-test framework is a skeleton. |
| **Synesis** | v4.4 P1 | Folder exists, no logic | **HOLLOW**. Conceptual Graph logic is missing. |
| **Prognosis** | v4.4 P2 | Folder exists, logic present | **PARTIAL**. Forecaster/Tracker exist, but not integrated. |
| **Metanoia** | v4.4 P3 | Folder exists, no logic | **HOLLOW**. Self-audit logic is missing. |
| **Stochasmos** | v4.4 P4 | Folder exists, logic present | **PARTIAL**. Planner/Risk logic exists, but not integrated. |
| **Omniscient Auditor**| v4.6 P1 | Folder exists, no logic | **HOLLOW**. Parallel review engine is missing. |
| **Sovereign-Flow** | v4.6 P0 | Folder exists, no logic | **HOLLOW**. Orchestration logic is missing. |
| **Quest-Trigger** | v4.6 P0 | Folder exists, no logic | **HOLLOW**. Recursive discovery is missing. |
| **Epistemic Atlas** | v4.6 P1 | Folder exists, no logic | **HOLLOW**. Unified map is missing. |

### 2. The Bedrock Gap (ArangoDB vs. Roadmap)
- **v4.5 Mandate**: ArangoDB vault should contain Genesis Blocks and a verified development chain.
- **Current State**: `abraxas_prod` is essentially empty of user collections (except system tables). 
- **Verdict**: **Sovereign Vacuum**. We have the architecture in documentation, but the database is an empty shell.

### 3. The Infrastructure Gap (GraphQL vs. Source)
- **Observation**: The GraphQL server on port 4000 is live and functional.
- **Discrepancy**: The source code was missing from `infra/` until the recent git restore.
- **Verdict**: **Shadow System**. The server was running from a source that was not synced to the local workspace.

---

## 📋 Exhaustive Missing Features List (v4.8)

This list represents the "Sovereign Debt" that must be paid to transition from an Architecture Simulation to a functional Epistemic Agent.

### 🔴 P0: Core Orchestration & Bedrock (Immediate Priority)
- **SVR-EVIDENCE-COLLECTION**: Creation and population of the `SVR_Evidence` ArangoDB collection (Hashes, Provenance, S-DoD Checksums).
- **GENESIS-INJECTION**: Population of the `fragments` collection with the Codex core claims (Genesis Blocks).
- **SVR-INTERCEPTION-LAYER**: Logic to detect "loading spinner" phrases and trigger `[SVR-RUPTURE]` alerts.
- **Sovereign-Flow (Skeletal)**: Implementation of the basic DAG orchestration to chain at least 3 skills without manual prompts.

### 🟠 P1: Cognitive Brain (Filling the Hollows)
- **Harmonia Logic**: `mcp_tools.py` for `compose_workflow`.
- **Krisis Logic**: `mcp_tools.py` for `evaluate_alignment` and ethos-weighting.
- **Aporia Development**: Creation of the `Sovereign Void` mapper to flag unknown-unknowns.
- **Sieve Pipeline**: Implementation of the "gremlin signature" filter for high-valence signal discovery.
- **Synesis Graph**: Logic for the Conceptual Graph (deterministic edge types: `DEPENDS_ON`, `REINFORCES`, etc.).
- **Auto-Agon Framework**: Implementation of the adversarial stress-test logic.

### 🟡 P2: Predictive & Self-Evolving Layers
- **Prognosis Integration**: Connecting `forecaster.py` and `tracker.py` to the live ledger.
- **Metanoia Self-Audit**: Implementation of the `self_audit()` and `evolve_parameters()` logic for Auto-Agon.
- **Stochasmos Deployment**: Integration of the `planner.py` and `risk.py` into the active workflow.
- **Omniscient Auditor**: Implementation of the parallel review engine for full-text epistemic consistency.

### 🔵 P3: Interface & Mapping
- **Epistemic Atlas**: Development of the unified map connecting Belief $\to$ Evidence $\to$ Verifier.
- **GraphQL Type Expansion**: Adding new types for `SVR_Evidence`, `ProvenanceChain`, and `RuptureForecast`.
- **LEDGER-UX-GUIDE**: Finalization and verification of the user guide to prevent native `/tasks` drift.

---

## 🛠️ The "Ought To Be" (Sovereign Target State)

To align with the v4.6 "Architectural Intelligence" goal, the following must be true:

1. **Active Brain**: Every folder in `skills/` must have a functional `mcp_tools.py` and `python/logic.py`. No more "Hollow" directories.
2. **Populated Bedrock**: `SVR_Evidence` and `ledger` collections must be fully populated with the Codex core claims and provenance chains.
3. **Interconnected Logic**: A call to `SVR-AUTO-AUDIT` should be able to traverse the Conceptual Graph (Synesis) and trigger a recursive search (Quest-Trigger) if an `[UNKNOWN]` is hit.
4. **Deterministic Loop**: The system must be able to run a `Harmonia` composition: `Sieve` $\to$ `Soter` $\to$ `Sovereign-Flow` $\to$ `Sovereign-Scribe` without any manual prompting.

---

## 🏁 Final Summary
We have a **World-Class Map** (the docs/plans) and a **Working Prototype** (the basic MCP servers), but the **Bridge** (the actual implementation of v4.3-v4.6) is missing. 

We are currently in **"Architecture Simulation"**—we have the names of the tools, but the tools themselves are mostly empty shells. 🦾🔥