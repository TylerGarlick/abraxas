# Project repos — each has its own .git, don't track inside mary-jane
projects/

# Local secrets and credentials
.credentials/

# OpenClaw runtime files
.openclaw/

# Temp and logs
logs/
tmp/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Secrets and sensitive files
secrets/
*.env
.env.local
.env.production
*.key
*.pem
secrets-master.key
*.sh
regenerate_*.sh
run_*.sh
generate_*.sh

# Ignore cloned projects (already under source control)
projects/
