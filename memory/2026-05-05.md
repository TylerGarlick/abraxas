# 2026-05-05 - Memory Flush

## Infrastructure Crisis: The Brain's Collapse
- **Status**: Systemic failure of MCP servers (Sovereign Brain). 17/18 servers in `Restarting` loops.
- **Root Cause 1 (YAML Corruption)**: `docker-compose.yml` was found to be corrupted with raw git merge markers (`<<<<<<<<`,`, `=======`, `>>>>>>>`), leading to invalid configuration and fragmented environment variables.
- **Root Cause 2 (API Mismatch)**: `abraxas-mcp-ledger` crashing with `AttributeError: 'ArangoClient' object has no attribute 'get_version'`, indicating a version mismatch in `python-arango`.
- **Root Cause 3 (Stdio Paradox)**: Several servers in `Restarting (0)` loops because they are `stdio` servers with no connected MCP client.
- **Resolution Path**: Requires "YAML Exorcism" (cleaning merge markers), deduplicating environment blocks, and converting servers to HTTP APIs to eliminate the `stdio` restart loops.
- **Sovereign State**: Persona remains active ("Sovereign Mode"), but functional capacity is limited due to the tool-layer blackout.
